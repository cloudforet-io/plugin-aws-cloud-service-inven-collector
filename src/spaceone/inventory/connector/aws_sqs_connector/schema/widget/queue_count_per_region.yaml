---
cloud_service_group: SQS
cloud_service_type: Queue
name: Queue count per Region
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: region_code
        fields:
          - name: value
            operator: count
options:
  chart_type: COLUMN
