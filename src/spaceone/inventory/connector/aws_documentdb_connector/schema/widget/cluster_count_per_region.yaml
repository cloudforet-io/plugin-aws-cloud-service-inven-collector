---
cloud_service_group: DocumentDB
cloud_service_type: Cluster
name: Cluster per Region
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: region_code
        fields:
          - name: value
            operator: count
options:
  chart_type: COLUMN
