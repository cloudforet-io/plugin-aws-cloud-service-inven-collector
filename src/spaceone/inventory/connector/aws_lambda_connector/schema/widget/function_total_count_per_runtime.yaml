---
cloud_service_group: Lambda
cloud_service_type: Function
name: Function Total Count per Runtime
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: data.runtime
        fields:
          - name: value
            operator: count
options:
  chart_type: DONUT
