---
cloud_service_group: GlobalAccelerator
cloud_service_type: Accelerator
name: Count by Account
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: account
        fields:
          - name: value
            operator: count
options:
  chart_type: DONUT
