---
cloud_service_group: DirectConnect
cloud_service_type: VirtualPrivateGateway
name: Count by Region
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: region_code
        fields:
          - name: value
            operator: count
options:
  chart_type: COLUMN
