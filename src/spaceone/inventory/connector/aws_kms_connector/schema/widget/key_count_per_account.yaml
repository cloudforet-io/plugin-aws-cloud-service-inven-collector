---
cloud_service_group: KMS
cloud_service_type: Key
name: Key per Account
query:
  aggregate:
    - group:
        keys:
          - name: account
            key: account
        fields:
          - name: value
            operator: count
options:
  chart_type: DONUT
