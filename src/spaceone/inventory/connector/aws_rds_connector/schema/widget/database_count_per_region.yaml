---
cloud_service_group: RDS
cloud_service_type: Database
name: Database per Region
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: region_code
        fields:
          - name: value
            operator: count
options:
  chart_type: COLUMN
