---
cloud_service_group: Repository
cloud_service_type: ECR
name: Repository per Account
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: account
        fields:
          - name: value
            operator: count
options:
  chart_type: DONUT
