---
cloud_service_group: SecretsManager
cloud_service_type: Secret
name: Secret per Account
query:
  aggregate:
    - group:
        keys:
          - name: account
            key: account
        fields:
          - name: value
            operator: count
options:
  chart_type: DONUT
