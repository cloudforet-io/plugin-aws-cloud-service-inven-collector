---
cloud_service_group: SecretsManager
cloud_service_type: Secret
name: Secret per Region
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: region_code
        fields:
          - name: value
            operator: count
options:
  chart_type: COLUMN
