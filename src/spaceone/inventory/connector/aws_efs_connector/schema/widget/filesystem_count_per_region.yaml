---
cloud_service_group: EFS
cloud_service_type: FileSystem
name: File System Count per Region
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: region_code
        fields:
          - name: value
            operator: count
options:
  chart_type: COLUMN
