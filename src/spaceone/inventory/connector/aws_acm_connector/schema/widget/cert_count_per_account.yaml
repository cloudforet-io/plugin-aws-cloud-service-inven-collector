---
cloud_service_group: CertificateManager
cloud_service_type: Certificate
name: Certificate per Account
query:
  aggregate:
    - group:
        keys:
          - name: name
            key: account
        fields:
          - name: value
            operator: count
options:
  chart_type: DONUT
