---
cloud_service_group: EC2
cloud_service_type: AMI
name: AMI per Account
query:
  aggregate:
    - group:
        keys:
          - name: account
            key: account
        fields:
          - name: value
            operator: count
options:
  chart_type: DONUT
