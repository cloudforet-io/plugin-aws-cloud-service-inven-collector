---
cloud_service_group: ECS
cloud_service_type: Cluster
name: Top Active Services per Cluster
query:
  aggregate:
    - unwind:
        path: data.services
    - group:
        keys:
        - name: name
          key: name
        fields:
        - name: value
          key: data.services
          operator: count
    - sort:
        key: value
        desc: true
options:
  chart_type: COLUMN
